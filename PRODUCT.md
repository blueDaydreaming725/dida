# Product

<!-- impeccable:product-schema 1 -->

## Platform

macos（原生菜单栏应用 / menu bar agent，非 web、非 iOS）

## Stack

Swift 6 + AppKit（主线程状态机、NSStatusItem、NSPanel）+ SwiftUI（面板与弹窗内容）。
Swift Package Manager 构建，脚本组装 .app（LSUIElement，无 Dock 图标），ad-hoc 签名。
委托决策：用户要求"性能极致、秒开秒关"，选择无依赖的原生 Swift 而非 Electron/Tauri。

## Users

- 主要用户：项目所有者本人——程序员，患过敏性结膜炎 + 干眼症，每天三次眼药水
  （「富马」抗过敏 + 「聚乙二醇」人工泪液，先富马、间隔约 5 分钟再聚乙二醇），
  长时间盯屏幕工作，需要定时提醒上药和定时休息。
- 次要用户：有类似用药/护眼节奏的其他人（药名、时间点、间隔均可配置）。

## Product Purpose

按用户作息定时提醒滴眼药水（两步流程：富马 → 间隔 → 聚乙二醇，均强制确认），
并在两次用药之间按固定间隔提醒护眼休息（20-20-20 法则）。
成功 = 不漏滴药、开会时一键静音不尴尬、应用本身零存在感（无窗口、无 Dock、低功耗）。

## Positioning

不是通用番茄钟：它内置"两步用药 + 两药间隔"的真实用药流程，
且以秒级开销常驻菜单栏（1Hz 单计时器 + 事件驱动），原生 Swift 无 WebView。

## Operating Context

- 用户全天在 macOS 上工作，常有会议（需一键静音 ⌥⌘M，默认 60 分钟自动恢复）。
- 随时可用快捷键 ⌥⌘B 主动进入休息（默认 5 分钟自动回来）。
- 笔记本可能合盖睡眠；唤醒后 30 分钟内错过的用药提醒立即补弹，超过则静默顺延。
- 弹窗不得抢键盘焦点（用户可能正在打字），但必须点击确认才会消失。

## Capabilities and Constraints

- 用药时间点：默认 11:00 / 15:00 / 20:00，可增删（1–6 个），可改药名。
- 两药间隔：默认 5 分钟，可调。
- 护眼休息提醒：默认每 20 分钟（15–60 可调），轻量横幅 20 秒自动消失。
- 全局快捷键：⌥⌘M 静音/恢复、⌥⌘B 休息/回来（Carbon RegisterEventHotKey，无需辅助功能权限）。
- 休息交互原则：休息是用户的，应用只记账不评判——随时可提前结束（再按 ⌥⌘B），
  结束时只报告实际休息时长并肯定短休的价值（"闭眼眨眼也有效"），绝不显示"未完成"；
  想延长可在面板点「再休 5 分钟」。主动休息默认 5 分钟（1–30 可调），
  最有效的休息方式是闭眼 + 有意识眨眼。
- 登录自启动：LaunchAgent 方式（绕开未签名应用的 SMAppService 限制）。
- 状态持久化：UserDefaults。
- 明确未决：无（v1 功能面已锁定）。

## Brand Commitments

- 名称「滴答」（滴 = 眼药水滴，滴答 = 计时），repo 名 `dida`。
- 视觉：原生 macOS 材质（毛玻璃、系统外观深浅色自适应），薄荷绿点缀仅用于主操作；
  禁止 AI 味装饰（渐变文字、卡片套卡片、表情符号充当图标——图标用 SF Symbols）。

## Evidence on Hand

- 用药事实来自用户自述：富马（抗过敏）、聚乙二醇（干眼），每日三次，先富马后聚乙二醇，间隔约 5 分钟。
- 休息间隔依据：20-20-20 法则（2023 年临床研究证实对数字视疲劳/干眼有效）；两药间隔约 10 分钟为医嘱惯例，用户自定 5 分钟。

## Product Principles

1. 零存在感：不工作时就只是一个菜单栏图标；性能开销小于感知阈值。
2. 坚持但不纠缠：用药必须确认，但绝不抢键盘焦点；错过有宽限与顺延。
3. 尊重会议：一键静音永远一键可达，且总会自动恢复（不怕忘）。
4. 节奏可配：时间点、间隔、时长全部可调，适配任何作息。

## Accessibility & Inclusion

- 弹窗按钮足够大，支持键盘 Tab + 回车（弹窗成为 key 时）。
- 所有窗口遵循系统深浅色与"减少透明度"（材质回退到系统默认）。
- 菜单栏图标有 accessibilityDescription。
