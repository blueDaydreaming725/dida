# 需求 TODO（已定方案，暂不实现）

## 1. 护眼提醒简化（v1.0.7 之上的改动）

现状：护眼弹窗有「已看远处 ✓」和「忙 · 5 分后再提」两个按钮。

改为：

- **去掉「忙/确认」双按钮**，弹窗本身只是一个提醒，不需要用户点按确认。
- **是否算休息以快捷键为准**：用户按下 ⌥⌘B 才开始休息，休息结束（自动回来或再按 ⌥⌘B）才算完成了一次护眼休息。
- **后续提醒逻辑**：
  - 提醒弹出后，若用户在窗口期内（约 5 分钟内）按 ⌥⌘B 完成休息 → 本轮结束，不再提醒，进入下一个 20 分钟计时；
  - 若没按 → **5 分钟后继续提醒**（反复，直到真正休息为止）。
- 弹窗动效保留（弹簧 + 震动 + 脉冲环）；按钮设计以 TODO ⑥ 为准。

## 2. 周报（参考 Apple 屏幕使用周报）

- **触发**：每周六上午自动生成（具体时间点待定，建议 9:00–10:00）。
- **内容**：统计上一周（周日至周五/周六）的休息数据：
  - 主动休息（⌥⌘B）次数与总时长；
  - 护眼微休息完成情况（触发数 / 实际休息数 / 比例）；
  - 每日分布（周日~周五/周六的柱状或表格式概览）；
  - 与上周对比（多了/少了多少）。
- **形式**：参考 Apple 屏幕使用时间的卡片式汇总（着色玻璃卡片 + 大数字 + 每日分布图）。
- **数据来源**：需新增本地休息记录存储（UserDefaults 或 SQLite/JSON 文件），从本版本开始默默积累数据。

## 3. 护眼间隔粒度与 20-20-20 规则对齐

现状：项目宣称遵循 20-20-20 法则，但设置里「护眼提醒」间隔的步进是 5 分钟（5–60），休息时长粒度为 1 分钟——口径不统一。

改为：

- 护眼间隔步进改为 5 分钟不变，但**默认值与文案锚定 20 分钟**（已经是 20，需在设置旁补充 20-20-20 的小注释）；
- 评估是否提供「严格 20-20-20」预设开关：开启后锁定 20 分钟间隔 + 20 秒微休息文案，隐藏细粒度调节；
- 微休息（看远处 20 秒）目前只是文案，无 20 秒计时——与 TODO ① 的快捷键休息语义合并考虑：按 ⌥⌘B 后是否内嵌一个 20 秒微休息倒计时。

## 4. 用药与休息的智能联动（互相影响）

把「滴眼药水」和「护眼休息」两个节奏打通，而不是各自独立计时：

- **滴药即休息**：滴眼药水（尤其聚乙二醇，闭眼吸收）本身算一次休息——
  两步用药确认完成后，把护眼休息计时刷新为「刚休息过」，从零重新计 20 分钟。
- **休息前预判用药**：每次护眼休息结束（或即将结束）时，检查下一个用药时间点——
  若距现在 ≤ 10 分钟，把这次护眼提醒和用药提醒**合并成一次**：
  提醒文案改为「顺便滴药：先富马，5 分钟后聚乙二醇」，避免用户刚坐下休息又被叫起来滴药。
- **合并规则细节**：
  - 只在用药窗口临近（≤10 分钟）且护眼提醒先到时合并；用药提醒永远优先弹出完整两步流程；
  - 合并后的确认仍分两步（滴药确认照旧），护眼计时随滴药完成自动刷新；
  - 「现在滴药」手动触发同样刷新休息计时。
- **待实现依赖**：与 TODO ① 的快捷键休息语义一并实现；周报（TODO ②）统计时，
  合并的「滴药休息」也应计入休息时长。

## 5. 熄屏（显示器关闭/睡眠）后停止计时

现状：合盖或屏幕熄灭后，应用的主计时器仍在跑，醒来时靠 30 分钟错过宽限处理——
可能补弹一批过期的用药/护眼提醒，休息时长也不反映真实用眼时间。

改为：

- **监听显示器熄灭/点亮**（NSWorkspace.screensDidSleepNotification / screensDidWakeNotification，
  与 didWakeNotification 并用）：
  - 熄屏期间：暂停用药与护眼计时（记录熄屏时刻）；
  - 点亮后：两个计时器基于「实际亮屏时长」重新排程，而不是从熄屏前的时间点顺延；
  - 熄屏时长本身应视为休息——护眼计时直接清零重算，与 TODO ④「滴药=休息」语义一致；
  - 用药时间点不同：它是绝对作息（11:00/15:00/20:00），熄屏跨过时间点仍按错过宽限规则补弹（保留现状）。
- **统计口径**：周报（TODO ②）的休息时长是否包含熄屏时间，实现时定一个口径并在 UI 注明。

## 6. 护眼提醒按钮修订（细化 TODO ①）

现状（v1.0.7）：护眼弹窗有两个按钮——「已看远处 ✓」和「忙 · 5 分后再提」。

改为**只有一个按钮**：

- 按钮文案：**「忙 · 本次只眨眼休息」**
- **点了** = 用户没空休息 → 弹窗收起，5 分钟后继续提醒（本轮未完成）；
- **没点** = 没休息 → 弹窗一直留着（不自动消失），持续等待；
- **主动按 ⌥⌘B** = 看远处/闭眼休息了 → 本轮结束，护眼计时清零重算，弹窗收起；
- 移除「已看远处 ✓」按钮——是否休息的唯一有效凭证是休息快捷键，与 TODO ① 的语义一致。
