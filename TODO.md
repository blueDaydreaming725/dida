# 需求 TODO（已定方案，暂不实现）

## 1. 护眼提醒简化（v1.0.7 之上的改动）

现状：护眼弹窗有「已看远处 ✓」和「忙 · 5 分后再提」两个按钮。

改为：

- **去掉「忙/确认」双按钮**，弹窗本身只是一个提醒，不需要用户点按确认。
- **是否算休息以快捷键为准**：用户按下 ⌥⌘B 才开始休息，休息结束（自动回来或再按 ⌥⌘B）才算完成了一次护眼休息。
- **后续提醒逻辑**：
  - 提醒弹出后，若用户在窗口期内（约 5 分钟内）按 ⌥⌘B 完成休息 → 本轮结束，不再提醒，进入下一个 20 分钟计时；
  - 若没按 → **5 分钟后继续提醒**（反复，直到真正休息为止）。
- 弹窗动效保留（弹簧 + 震动 + 脉冲环），按钮区整体移除。

## 2. 周报（参考 Apple 屏幕使用周报）

- **触发**：每周六上午自动生成（具体时间点待定，建议 9:00–10:00）。
- **内容**：统计上一周（周日至周五/周六）的休息数据：
  - 主动休息（⌥⌘B）次数与总时长；
  - 护眼微休息完成情况（触发数 / 实际休息数 / 比例）；
  - 每日分布（周日~周五/周六的柱状或表格式概览）；
  - 与上周对比（多了/少了多少）。
- **形式**：参考 Apple 屏幕使用时间的卡片式汇总（着色玻璃卡片 + 大数字 + 每日分布图）。
- **数据来源**：需新增本地休息记录存储（UserDefaults 或 SQLite/JSON 文件），从本版本开始默默积累数据。
